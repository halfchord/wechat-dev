spring:
  application:
    name: main-service
  cloud:
    nacos:
      discovery:
        server-addr: 119.45.220.62:8848
        username: nacos
        password: nacos
        group: DEFAULT_GROUP  # 配置分组（默认DEFAULT_GROUP，可根据需要修改）
      config:
        server-addr: 119.45.220.62:8848
        file-extension: yml
        username: nacos
        password: nacos
        group: DEFAULT_GROUP
