server:
  port: 1000
  tomcat:
    uri-encoding: UTF-8
    max-swallow-size: -1 #不限制请求大小

#日志级别
logging:
  level:
    root: info

spring:
  data:
    redis:
      host: 119.45.220.62
      port: 5379
      database: 0
  mail:
    default-encoding: UTF-8
    host: smtp.163.com
    port: 465
    username: 15180680576@163.com
    password: EFfQZYE9qfNrUbBm
    properties:
      mail:
        smtp:
          auth: true
          ssl:
            enable: true
            required: true

  cloud:
    gateway:
      discovery:
        locator:
          enabled: true   # 动态路由，从注册中心根据微服务的名称获得目标节点的地址
      routes: # 配置路由规则（数组/list）
        - id: authRoute   # 每一项路由规则都有一个唯一的id编号，可以自定义
          uri: lb://auth-service    # lb=负载均衡，会动态寻址
          predicates:
            - Path=/a/**,/passport/**
        - id: fileRoute
          uri: lb://file-service
          predicates:
            - Path=/f/**,/file/**,/static/**,/speech/**
        - id: mainRoute
          uri: lb://main-service
          predicates:
            - Path=/m/**,/userinfo/**,/friendRequest/**,/friendship/**,/friendCircle/**,/comment/**,/chat/**
      globalcors:
        cors-configurations:
          '[/**]':
            allowedOriginPatterns: "*"
            allowedHeaders: "*"
            allowedMethods: "*"
            allowCredentials: true
    nacos:
      discovery:
        server-addr: 119.45.220.62:8848
